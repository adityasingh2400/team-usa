export default function MomentPlaceholderPage() {
  return (
    <main className="step-shell flex min-h-screen items-center justify-center py-10">
      <section className="surface max-w-2xl p-8">
        <p className="text-sm font-semibold uppercase tracking-[0.18em] text-slate-500">Handoff ready</p>
        <h1 className="mt-3 text-3xl font-bold text-slate-950">Moment engine placeholder</h1>
        <p className="mt-4 text-slate-600">
          The onboarding engine now hands a populated archetype reveal into this route. Aditya can replace this
          placeholder with the Paralympic-first moment flow.
        </p>
      </section>
    </main>
  );
}
